services:
  postgresql:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tournament
    volumes:
      - ./postgres:/var/lib/postgresql
    networks:
      - db_network

networks:
  db_network:
    driver: bridge
